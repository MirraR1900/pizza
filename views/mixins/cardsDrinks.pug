mixin CardsDrinks()
  div(class="boxMenuDrinks" id="drinks")
    each val, index  in drink
      .card
         .boxImg
           img(class="drinkImg" src=val.img alt="")
         p(class="nameDish" value=val.title)= val.title 
         p(class="descriptionDish")= "Состав: " + val.description
         p(class="weightDush")= "Объем: " + val.volume + " л."
         p(class="priceDish" value=val.price)= "Цена: " + val.price  + " руб" 
         .addProduct
          p.boxbtnAddBasket
            button(class="btnAddBasket") Добавить в корзину  